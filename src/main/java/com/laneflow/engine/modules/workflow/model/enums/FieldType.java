package com.laneflow.engine.modules.workflow.model.enums;

public enum FieldType {
    TEXT, TEXTAREA, NUMBER, DATE, DATETIME,
    SELECT, MULTISELECT, CHECKBOX, RADIO,
    FILE, IMAGE, PHOTO, AUDIO, DOCUMENT, VIDEO, GENERIC
}
